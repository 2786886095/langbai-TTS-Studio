# 更新日志

本项目采用语义化版本号。尚未发布的变化记录在“未发布”部分。

## 未发布

## 1.2.11 - 2026-08-24

### 新增

- GPT-SoVITS 多人配音新增自适应 GPU 调度：依据 NVIDIA 显存、负载和温度自动安排 1–4 个隔离 Worker；默认采用激进并发，在 10GB 以上显卡尝试至少双 Worker和 98% 显存上限，任务队列会显示实际 GPU Worker 数。

### 改进

- 多人台词改为按角色集中调度，生成顺序可并发但最终仍严格按剧本序号合并，避免角色交替时反复卸载和载入 GPT／SoVITS 权重。
- 额外 Worker 遇到 CUDA 显存不足或运行中显存、温度越界时会自动退出，其未完成台词转回主 Worker 续作，不再让整条长音频失败。
- 关闭或取消多人任务时会同时终止该任务启动的全部推理 Worker，防止后台残留模型进程。

### 修复

- Windows 桌面端加入全局单实例锁；重复启动时不再创建第二个软件窗口或后端服务，而是恢复并聚焦已经打开的主窗口。

## 1.2.10 - 2026-08-14

### 修复

- GPT-SoVITS 多人配音现在会检测分段内部的异常长静音，先更换种子自动重试；最后一次仍异常时将长静音压缩为自然停顿，避免数秒至数十秒的中断进入最终音频。
- 最终多人音频增加流式长静音保险处理，保持固定内存占用，适用于不限时长的长音频。

## 1.2.9 - 2026-08-14

### 改进

- GPT-SoVITS 多人配音的“稳定优先”“自然平衡”“表现力优先”三档质量预设统一使用 `1.1` 倍语速。
- 安装包版本与后端 API 自报版本统一为 `1.2.9`，避免更新与诊断信息出现版本不一致。

## 1.2.8 - 2026-08-14

### 新增

- GPT-SoVITS 多人配音新增“稳定优先”“自然平衡”“表现力优先”三档质量策略，默认使用稳定优先。
- 多人任务新增逐片段质量门控：检测无声、异常响度、削波、静音占比和文本／时长失配，不合格片段会更换种子自动重试。

### 改进

- GPT-SoVITS 多人模式默认进入专注工作区，折叠引擎选择、模式选择和格式说明，让剧本文本与角色映射获得更大的可用高度；顶部设置可随时展开。
- 稳定模式为每个角色生成固定种子，并统一使用原速、较低温度与更短的内部停顿，减少同一角色跨台词的音色漂移和机械断句。
- 不同模型版本产生的 32kHz／48kHz 音频改用 SoXR HQ 重采样后再合并，避免原线性插值造成的额外损失。

### 修复

- 自动裁剪生成片段首尾的异常长静音，避免多秒空白直接进入最终长音频。

## 1.2.7 - 2026-08-14

### 新增

- GPT-SoVITS 创作台新增单人／多人模式切换；多人剧本按 `【角色名】：台词` 识别角色，旁白使用 `【旁白】：内容`。
- 每个角色可独立匹配已保存的 GPT-SoVITS 角色声音和参数预设；角色声音保存 GPT/SoVITS 权重、参考音频、精确参考文本、参考语言和模型版本。
- 多人任务按剧本物理行顺序生成，每行保持独立语句，并以一个全局台词间隔合并为最终 WAV；默认间隔为 280ms。
- 所有可编辑文本框提供原生右键撤销、重做、剪切、复制、粘贴、删除和全选菜单。

### 改进

- GPT-SoVITS 的 `top_p` 默认值调整为 `0.7`。
- GPT-SoVITS 参数预设不再保存或覆盖角色声音中的模型、参考音频、参考文本、参考语言和版本。
- 多人模式在 1920×1080 与 1180×720 下均采用无横向滚动布局；进入多人配置时自动收起空生成队列。

### 修复

- 多人剧本中未带标签的非空行会明确列出行号与原文，并在修正前阻止提交。

## 1.2.6 - 2026-07-22

### 新增

- 音频库与历史记录可删除任务；有输出音频时明确提供“仅删除记录”和“记录与音频一起删除”两种选择。

### 改进

- 默认输出目录改为软件根目录下的 `output`，设置页仍可选择其他绝对路径。
- 任务名称默认从正文开头自动生成，导入、粘贴和编辑正文时会同步更新尚未手动修改的名称。
- 每次启动均以空生成队列开始；上次中断的任务保留在历史记录中，可按需重试。
- 音频库、历史记录和任务队列改为自适应卡片布局，操作按钮自动换行，不再需要左右拖动。

### 修复

- 修复 GPT-SoVITS 版本默认参数标记 `sample_steps_auto` 被后端误判为未知参数的问题。

## 1.2.5 - 2026-07-20

### 新增

- 三个引擎均可在推理参数抽屉中保存多套独立参数预设，并可随时选择、更新或删除；预设跨项目和软件重启保留。

### 改进

- GPT-SoVITS 按模型版本应用采样默认值：v3 默认 32 步、v4 默认 8 步，v1、v2 与 Pro 系列继续由上游忽略不适用的采样步数。
- `auto` 模式先根据权重路径即时调整界面，再以模型加载后的真实版本二次校正；手动修改过的采样步数不会在切换版本时被无条件覆盖。
- 非 v3 权重自动关闭仅由 v3 支持的超采样选项，避免提交无效或不兼容参数。

## 1.2.4 - 2026-07-20

### 新增

- 生成完成后可直接在创作台的任务行点击“试听”，使用软件内置播放器播放最终音频。
- 完成任务提供“打开位置”，可在 Windows 资源管理器中选中实际输出音频。
- 任务队列现在保留已完成任务，并提供与音频库一致的试听和文件定位操作。

### 改进

- 播放和定位操作会通过任务输出接口重新解析真实文件，避免使用过期或不完整的前端路径。
- 完成任务操作在窄窗口下保持紧凑布局，并避免产生横向滚动。

## 1.2.3 - 2026-07-20

### 修复

- 修复部分 GPT-SoVITS 整合包把 `custom.device` 保存为空字符串后，社区模型加载时报 `don't know how to restore data location` 的问题；空设备会按本机 CUDA 可用性自动选择 CUDA 或 CPU。
- 空的设备、BERT 与 CNHuBERT 参数不再覆盖上游配置中的有效默认路径。
- 取消运行中的生成任务会立即终止当前引擎推理进程，不再等待整个分段生成结束。

### 改进

- 创作台顶部为运行中或排队中的任务显示醒目的“取消生成”按钮。
- 生成队列支持取消运行中和排队中的任务，并在确认框中说明已完成分段会保留、可断点重试。
- 增加空设备自动修复、工作进程中断和运行任务取消的回归测试。

## 1.2.2 - 2026-07-20

### 修复

- 修复社区模型完成解压后，因 `installed\auto` 版本目录尚未创建而无法移动到最终安装位置的 Windows `[WinError 3]`。
- 增加从下载、解压、创建版本目录、原子移动到写入模型清单的完整安装回归测试。

## 1.2.1 - 2026-07-20

### 修复

- 修复社区模型下载地址跳转至 ModelScope 官方 CDN 后被误判为不受信任地址的问题；所有重定向仍会在连接前执行严格域名与 HTTPS 校验。
- VoxCPM2 训练配置改为默认占满工作区，进度、任务与完整日志改为大尺寸右侧抽屉，解决表单过小和固定侧栏浪费空间的问题。

### 验证

- 使用当前编译后端实际下载社区模型首个数据块，确认任务进入下载状态且不再触发地址拒绝。
- Windows 自动构建完成后验证后端测试、前端构建、安装包生成和打包进程清理。

## 1.2.0 - 2026-07-20

### 新增

- 左侧统一“模型训练”入口，可选择 GPT-SoVITS 或 VoxCPM2，并提供分步中文训练指引、官方文档和 B 站学习入口。
- VoxCPM2 软件内 LoRA 与全量 SFT 训练，支持本地数据清单、完整参数、日志、检查点、安全停止和恢复训练。
- GPT-SoVITS 官方训练工作台由软件检测、启动、嵌入和终止；拦截上游 `inbrowser=True`，不再弹出外部浏览器。
- 三个引擎各自独立的软件内可视化运行终端，显示启动命令、PID、日志和运行状态。
- GPT-SoVITS 社区模型支持 Hugging Face、B 站/网盘来源入口和本地权重智能识别配对。

### 改进

- 完整推理参数改为从右侧弹出的宽抽屉，释放创作区空间并改善搜索与分组操作。
- 软件更新可在全局显示明显提示，并在退出或安装更新前检测引擎、生成与训练活动。
- 仅在存在活动进程或任务时询问是否终止；关闭时对训练器执行安全停止并清理子进程。
- 修复 VoxCPM2 超长本地模型路径撑破训练布局的问题。
- 优化运行状态轮询和本地扫描，减少界面卡顿与重复请求。

## 1.1.0 - 2026-07-19

### 新增

- 按引擎独立保存、编辑、删除和应用角色声音配置。
- GPT-SoVITS 角色声音成对保存 GPT `.ckpt`、SoVITS `.pth`、参考音频、精确文本和模型版本。
- GPT-SoVITS 社区模型广场，支持作品/语言分类、搜索、安全下载、进度展示和下载后创建角色声音。

### 改进

- IndexTTS2 与 VoxCPM2 明确采用参考音频/音色设计，不再把已绑定程序误显示为通用角色权重。
- 删除界面缩放设置与缩放快捷键，保留舒适/紧凑信息密度。
- Setup 安装包使用明确文件名，并固定到标准安装目录，避免误覆盖源码仓库。
- 社区模型仅导入权重、参考音频和说明文件；拒绝越界 ZIP 路径、脚本与可执行文件。
- 长音频不再设置软件时长或项目文本上限；最终音频按分段流式写入，超过传统 WAV 容量时自动使用 RF64。

## 1.0.0 - 2026-07-19

### 新增

- IndexTTS2、VoxCPM2 与 GPT-SoVITS 的统一 Windows 创作工作台。
- 每个任务独立选择一个引擎，并提供完整原生参数及中文用途说明。
- 长文本分段、任务恢复、音频合并、项目持久化、历史记录与音频库。
- 应用内引擎安装、模型按需选择、诊断导出和 GitHub 自动更新入口。
- 舒适与紧凑两种界面密度、窗口缩放、键盘快捷键与高 DPI 布局。

### 安全与可靠性

- 引擎代码和安装工具固定版本并进行 SHA-256 校验。
- 本地音频通过受控 Electron 协议播放，渲染进程不获得任意文件读取权限。
- 项目、设置、安装状态和诊断包使用原子写入与明确失败状态。
